// Shared layout constants — used by both styles.js (CSS) and the behavior
// modules (position.js, resize.js, drag-dismiss.js) that measure/clamp
// against the same numbers, so there's exactly one place to change them.

export const BOX_WIDTH = 340;
export const EDGE_MARGIN = 12;
export const GAP = 8;
export const MOBILE_BREAKPOINT = 640;
export const DISMISS_DRAG_THRESHOLD = 80;
export const RESIZE_HANDLE_THICKNESS = 8;
export const MIN_BOX_WIDTH = 260;
export const MIN_BOX_HEIGHT = 160;
