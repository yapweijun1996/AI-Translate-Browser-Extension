{"id":"T-001","epic":"E1","milestone":"M1","title":"Init npm project + Vite + CRXJS build tooling","status":"done","depends_on":[],"details":"package.json, vite.config with @crxjs/vite-plugin (fallback: esbuild script if CRXJS blocks). Output dist/ loadable unpacked. Add npm scripts: dev, build. Node 20+."}
{"id":"T-002","epic":"E1","milestone":"M1","title":"Write manifest.json (MV3, minimal permissions)","status":"done","depends_on":["T-001"],"details":"MV3. permissions: storage, contextMenus, scripting, activeTab. host_permissions: [] . default_locale: en. name/description via __MSG_ext_name__/__MSG_ext_description__. Placeholder icons. See SPEC §3 §7."}
{"id":"T-003","epic":"E1","milestone":"M1","title":"Scaffold 4 contexts: service worker, content script, popup, options","status":"done","depends_on":["T-002"],"details":"background/service-worker.js, content/main.js, popup/, options/ — each just logs a heartbeat and (for content) responds to a PING message from the worker. Verify message round-trip works."}
{"id":"T-004","epic":"E5","milestone":"M1","title":"Scaffold _locales for en, zh_CN, ms, ja, vi, ko","status":"done","depends_on":["T-002"],"details":"messages.json per locale with ext_name + ext_description. English text as placeholder in non-en files is OK at this stage. See SPEC §8 key conventions."}
{"id":"T-005","epic":"E1","milestone":"M1","title":"Lint/format config + CI workflow","status":"done","depends_on":["T-001"],"details":"ESLint + Prettier. GitHub Actions: install, lint, build on every PR. Fail on lint errors."}
{"id":"T-006","epic":"E2","milestone":"M2","title":"Selection detector in content script","status":"done","depends_on":["T-003"],"details":"Port REFERENCE-SNIPPETS §1 selection detector: mouseup+touchend debounced 250ms, selectionchange clears state, ignore <2 chars or collapsed, capture bounding rect. Emit internal event {text, rect}."}
{"id":"T-007","epic":"E2","milestone":"M2","title":"Shadow DOM UI host + trigger icon","status":"done","depends_on":["T-006"],"details":"One host element appended to documentElement, attachShadow, CSS reset inside. Floating icon appears near selection end on selection event; disappears on selection collapse/scroll. Must not steal focus or break copy."}
{"id":"T-008","epic":"E2","milestone":"M2","title":"Translation modal box (anchored) + dismissal","status":"done","depends_on":["T-007"],"details":"Click icon → modal anchored to selection rect (flip if near viewport edge). Shows source text + loading state + result area + Explain button (hidden for now). Dismiss: Esc, outside mousedown, × button. Port positioning from REFERENCE-SNIPPETS §7."}
{"id":"T-009","epic":"E2","milestone":"M2","title":"Mobile bottom-sheet variant + drag-to-dismiss","status":"done","depends_on":["T-008"],"details":"Viewport <640px: modal renders as bottom sheet, drag handle to dismiss (REFERENCE-SNIPPETS §7)."}
{"id":"T-010","epic":"E2","milestone":"M2","title":"Context capture: surrounding paragraph","status":"done","depends_on":["T-006"],"details":"Walk up from selection commonAncestorContainer, take innerText, truncate ~1200 chars (REFERENCE-SNIPPETS §2). Sent along with translate request; needed later by Explain."}
{"id":"T-011","epic":"E2","milestone":"M2","title":"Message protocol + mock translator in service worker","status":"dropped","depends_on":["T-003"],"details":"DROPPED (user decision, 2026-07-03): skip the mock translator, go straight to real engines starting at T-013. The message protocol itself (MSG.TRANSLATE/EXPLAIN, {ok,data}/{ok,error} envelope) is already defined in shared/messages.js and documented in docs/ARCHITECTURE.md as part of T-001..T-010 wiring — only the mock-response part of this task is being skipped."}
{"id":"T-012","epic":"E2","milestone":"M2","title":"E2 exit-criteria test pass on 10 real sites","status":"todo","depends_on":["T-008","T-009","T-010","T-023"],"details":"Manual QA checklist: news site, GitHub, MDN, Wikipedia, Twitter/X, Gmail, a React SPA, a strict-CSP site, an iframe-heavy site, one RTL page. Record findings in docs/qa/e2-checklist.md. Depends on T-023 (real engines wired in) instead of a mock — T-011 was dropped, so this is the first point a real translation result exists to test against."}
{"id":"T-013","epic":"E3","milestone":"M3","title":"Engine adapter interface + registry","status":"done","depends_on":["T-003"],"details":"interface: translate(text, targetLang, opts) / explain(...) / capabilities() / isAvailable(). Registry picks active engine from settings. All in service worker. See docs/ENGINES.md. Depends only on the worker skeleton (T-003) existing — T-011 (mock translator) was dropped; the message protocol it would have formalized is already defined in shared/messages.js + docs/ARCHITECTURE.md."}
{"id":"T-014","epic":"E3","milestone":"M3","title":"Trial gateway adapter (REFERENCE-SNIPPETS §3)","status":"done","depends_on":["T-013"],"details":"Port REFERENCE-SNIPPETS §3: XOR decryptKey (seed 20260515) + SSE streaming parse for /v1/responses, model gpt-5.4-mini, reasoning.effort low. Bundled cipher string only — plaintext key NEVER in repo. AbortSignal timeout 30s."}
{"id":"T-015","epic":"E3","milestone":"M3","title":"Chrome on-device Translator adapter","status":"done","depends_on":["T-013"],"details":"Feature-detect Translator + LanguageDetector APIs (Chrome 138+). Handle model-download progress state. capabilities(): translate only, no explain. Graceful unavailable state."}
{"id":"T-016","epic":"E3","milestone":"M3","title":"BYOK adapter #1: Gemini","status":"done","depends_on":["T-013"],"details":"generateContent REST with user key from chrome.storage.local. Then clone pattern for OpenAI (T-017) and DeepSeek (T-018)."}
{"id":"T-017","epic":"E3","milestone":"M3","title":"BYOK adapter #2: OpenAI","status":"done","depends_on":["T-016"],"details":"chat.completions or responses API with user key."}
{"id":"T-018","epic":"E3","milestone":"M3","title":"BYOK adapter #3: DeepSeek","status":"done","depends_on":["T-016"],"details":"OpenAI-compatible endpoint with user key."}
{"id":"T-019","epic":"E3","milestone":"M3","title":"Options page: engine, keys, target language, UI language","status":"done","depends_on":["T-013"],"details":"Engine picker, per-provider key fields (masked, stored chrome.storage.local), target-language default, UI-language note. All strings via chrome.i18n."}
{"id":"T-020","epic":"E3","milestone":"M3","title":"IndexedDB translation cache in service worker","status":"done","depends_on":["T-013"],"details":"Key: v{PROMPT_VERSION}::{targetLang}::{engine}::{sha256(text) short}. LRU cap. Version-bump invalidation per REFERENCE-SNIPPETS §6. See SPEC §6."}
{"id":"T-021","epic":"E3","milestone":"M3","title":"Probe live gateway quota-error shape + central error mapper","status":"done","depends_on":["T-014"],"details":"Deliberately exhausting/probing the live gateway daily limit was descoped per owner instruction (2026-07-03) — the gateway already has quota-handling logic server-side and forcing it isn't this project's job; the only other tester (the owner) confirmed no other users are on it yet, so there was no shared-impact concern, but triggering it was explicitly waved off anyway. Built the central error mapper instead (background/error-mapper.js): mapHttpError({status,bodyMessage,providerName,isTrialGateway}) + mapNetworkError + extractErrorMessage. All 4 engines (trial-gateway/gemini/openai/deepseek) refactored to call it instead of each duplicating status-code judgment calls. isTrialGateway:true (trial-gateway.js only) maps 429->trial_quota_exhausted; BYOK engines map 429->plain quota. Classification is interim/best-effort by standard HTTP semantics (401/403 auth, 429 quota-or-trial_quota_exhausted, else gateway_error) since the live shape was never confirmed — flagged clearly in docs/ENGINES.md as the one thing to revisit if the live gateway differs from standard HTTP conventions."}
{"id":"T-022","epic":"E3","milestone":"M3","title":"Trial-quota BYOK upsell UI","status":"done","depends_on":["T-021","T-019"],"details":"On trial_quota_exhausted: modal shows upsell (set up own key → opens options; try again tomorrow; on-device fallback if available). BYOK users' own quota errors show normal message, NOT upsell. See SPEC §4 §9."}
{"id":"T-023","epic":"E3","milestone":"M3","title":"Confirm cross-engine integration (re-scoped from \"replace mock\")","status":"done","depends_on":["T-014","T-015","T-020"],"details":"RE-SCOPED 2026-07-03: original title/scope (\"replace mock\") is stale — T-011 (mock translator) was dropped before any mock ever existed, and real engine results have been live in the modal since T-014. \"Modal translate path uses active engine + cache\" has been true since T-020; the trial-quota upsell path (T-022) already exercises this end-to-end with mocked responses. What is NOT yet verified: a holistic pass with all 5 REAL registered adapters (trial-gateway/on-device/gemini/openai/deepseek) together — switching engineId across all of them, confirming GET_CAPABILITIES reflects each switch, and confirming cache isolation holds across all 5, not just the fake-engine case tested in T-020. \"Streaming: render tokens incrementally when engine supports SSE\" is DEFERRED to ROADMAP v1.0 (already implied by its existing Performance bullet \"modal open->first translated token < 1.5s\") — SSE today is a transport-layer necessity (trial-gateway.js avoids Cloudflare edge timeout) but the modal only ever renders the fully-assembled result, no incremental UI. Not needed for MVP: translations are short selected phrases, not long documents (full-page translation is explicitly out of scope per SPEC §10), so the latency benefit of streaming UI is marginal for the actual use case."}
{"id":"T-024","epic":"E4","milestone":"M4","title":"Explain prompt + strict-JSON schema + loose parser","status":"done","depends_on":["T-023"],"details":"Port REFERENCE-SNIPPETS §5: schemaVersion, buildPrompt (CEFR-graded examples, simpler-vocab rule), parseJsonLoose fallback. LLM engines only."}
{"id":"T-025","epic":"E4","milestone":"M4","title":"Explain rendering in modal","status":"done","depends_on":["T-024"],"details":"Render explain payload: headword + POS/CEFR badges, dual-language definitions, graded examples, collapsible collocations/word-family/synonyms/antonyms/memory-tip. Escape all HTML. Explain button shown unconditionally after any successful translation (capability gating deferred to T-026, per its own scope) except the trial-quota upsell's on-device retry, where the engine is deterministically known not to support explain."}
{"id":"T-026","epic":"E4","milestone":"M4","title":"Explain cache + engine-capability gating","status":"done","depends_on":["T-024"],"details":"Cache key: explain::v{SCHEMA_VERSION}::{origin}::{normalized phrase}::{targetLang}. Hide/disable Explain button when active engine lacks explain capability, with i18n hint. Implemented as background/explain-cache.js (IndexedDB/LRU, mirrors translation-cache.js) wired into registry.js's explain(); main.js queries GET_CAPABILITIES before offering the button and shows it disabled (not hidden) with an i18n hint when canExplain is false, failing closed on error."}
{"id":"T-027","epic":"E5","milestone":"M5","title":"i18n completeness: translate all 6 locales for real","status":"done","depends_on":["T-019","T-022","T-025"],"details":"RE-SCOPED 2026-07-03: every task that added an i18n key (T-018, T-020..T-022, T-025, T-026) wrote a real translation directly into all 6 locale files as it went, rather than English placeholders — confirmed no commit ever carried the docs/I18N.md 'i18n-placeholder' tag. Verified 2026-07-03: all 6 locales have identical 46-key sets; the only value identical across every locale is ext_name (the brand name, deliberately kept as-is). REMAINING GAP (owner-acknowledged, not blocking): every non-English string is LLM-translated only — no native-speaker human review pass has happened yet for zh_CN/ja/ko/ms/vi. Tracked as a pre-launch follow-up, revisit before T-031 (store listing) / T-032 (submission)."}
{"id":"T-028","epic":"E5","milestone":"M5","title":"CI i18n check: no hardcoded strings, no missing keys","status":"done","depends_on":["T-005","T-004"],"details":"Script: every getMessage key exists in all 6 messages.json; grep heuristic for hardcoded UI literals in content/popup/options. Wire into CI. Implemented as scripts/check-i18n.mjs (npm run lint:i18n, wired into .github/workflows/ci.yml): checks key parity across all 6 locales, flags keys referenced in code/manifest/HTML but undefined in en (typo detection), flags en keys never referenced (warning only), and a grep heuristic (2+ alphabetic words) over content/popup/options JS string literals + HTML text nodes lacking data-i18n, with a `// i18n-ok` suppression escape hatch. Fixed a real gap it found: explain_section_* keys (T-025) didn't match any documented I18N.md prefix — added explain_section_ to the table and the checker's allowlist. Manually verified by injecting one violation of each of the 4 kinds and confirming each was caught, then reverting."}
{"id":"T-029","epic":"E6","milestone":"M5","title":"Context menu: Translate selection","status":"done","depends_on":["T-023"],"details":"contextMenus entry (i18n title) → same pipeline as icon click. Implemented: service-worker.js creates the 'translate-selection' item (contexts: ['selection']) idempotently in onInstalled (removeAll then create, so a re-fired onInstalled never throws on a duplicate id); its onClicked handler sends a new worker→content MENU_TRANSLATE_SELECTION message via chrome.tabs.sendMessage (targeted at just that tab, not the extension-wide broadcast) carrying info.selectionText. main.js's new onMessage listener re-derives the live selection rect and calls the same translateSelection() the trigger icon uses. Incidental cleanup: removed the OPEN_OPTIONS message type from shared/messages.js — it was dead code (both popup.js and main.js already called chrome.runtime.openOptionsPage() directly, never through a message)."}
{"id":"T-030","epic":"E6","milestone":"M5","title":"Icons + branding","status":"done","depends_on":[],"details":"16/32/48/128 px extension icons, trigger-icon glyph, consistent palette. Update manifest. Implemented: public/icons/icon-{16,32,48,128}.png reuse the exact glyph already drawn in src/content/trigger-icon.js's ICON_SVG (the 'A' + CJK-character translate motif) on the same #2563eb brand blue circle, so the toolbar icon and the in-page trigger icon are visually the same mark — the trigger-icon glyph itself already existed from T-007, so this task's real work was rasterizing it to the 4 required PNG sizes and wiring manifest.json's icons + action.default_icon fields. Icons generated via an SVG->canvas->PNG rasterization in a real browser (not a static asset pipeline dependency) and verified by decoding each file back via Image() in-browser to confirm valid, correctly-sized PNGs. No new palette needed — #2563eb was already the established primary color across trigger-icon.js/modal.js."}
{"id":"T-031","epic":"E6","milestone":"M5","title":"Privacy policy + Web Store listing (6 locales)","status":"done","depends_on":["T-027"],"details":"State exactly what text is sent to which engine and when; zero-retention claims only where true. Store listing copy in all 6 languages. Implemented under docs/store/: PRIVACY-POLICY.md (English canonical, verified per-engine data flow against the real code — trial-gateway/on-device/gemini/openai/deepseek — confirmed with the owner 2026-07-03 that the trial gateway tracks usage counts only, not request/response content, so the 'not stored' claim is accurate) + privacy-policy-<locale>.md full translations for zh_CN/ja/ko/ms/vi; listing-<locale>.md (title/summary/description) for all 6 locales; SUBMISSION-NOTES.md for the Developer Dashboard fields that aren't locale-specific (single purpose, permission justifications, data-usage disclosure — English only, that's how the Dashboard works). Incidental fix discovered while justifying permissions: manifest.json's 'scripting' and 'activeTab' were requested but never used anywhere in the code — removed (owner-approved) in the same change; SPEC.md/CLAUDE.md updated to match. Publishing the policy to a real public URL and capturing store screenshots are manual steps still owed before actual submission (T-032)."}
{"id":"T-032","epic":"E6","milestone":"M5","title":"MVP end-to-end QA + store submission","status":"todo","depends_on":["T-028","T-029","T-030","T-031","T-026","T-022"],"details":"Run ROADMAP v0.x exit criteria: fresh install → translate on 10 sites → hit trial quota → switch to BYOK → explain works. Fix blockers, submit to Chrome Web Store."}
{"id":"T-033","epic":"E6","milestone":"M5","title":"Popup: settings entry point + live engine status","status":"done","depends_on":["T-013","T-019"],"details":"Added mid-M3 per direct user request (2026-07-03) after seeing the M1 placeholder popup — not originally scoped as its own task. Popup shows the currently active engine (via GET_CAPABILITIES, friendly name from a shared engine-label map) and a Settings button that opens the options page, plus a one-line usage hint for first-time users. Replaces the M1 popup_status_ready placeholder."}
